import { PLATFORM_PAGE_DEFAULT } from '../../../../design-system/platformTokens';

/** Conteneur page profil étudiant — aligné sur les autres modules (documents, SRF, etc.). */
export const STUDENT_PROFILE_PAGE_ROOT = PLATFORM_PAGE_DEFAULT;
