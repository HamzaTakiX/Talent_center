/** Layout responsive — module Announcements (aligné Internship Offers). */

export const ANNOUNCEMENTS_PAGE_ROOT =
  'mx-auto flex w-full min-w-0 max-w-full flex-col gap-4 scroll-mt-4 pb-2 font-inter max-[429px]:gap-3.5 sm:max-w-[1600px] sm:gap-6 md:gap-8 sm:pb-4';

export const ANNOUNCEMENTS_FEED_PAGE_SIZE = 8;

/** Bouton retour — pill avec fond visible (aligné offres de stage). */
export const STUDENT_BACK_NAV_BUTTON =
  'student-back-nav back-button inline-flex h-9 max-w-full items-center gap-2 rounded-full border px-2 py-1.5 text-sm font-medium sm:h-10 sm:gap-2.5 sm:px-2.5';
