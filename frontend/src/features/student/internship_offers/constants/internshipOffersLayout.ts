/** Classes layout responsive — module Internship Offers (design system admin). */
import { STUDENT_PAGE_CONTAINER_WIDE } from '../../design-system/studentTokens';

export const INTERNSHIP_OFFERS_PAGE_ROOT = STUDENT_PAGE_CONTAINER_WIDE;

export const INTERNSHIP_OFFERS_PAGE_HEADER =
  'student-module-hero flex w-full min-w-0 max-w-full flex-col gap-3 sm:gap-3.5';

/** Bouton retour — pill avec puce icône et hover brand. */
export const STUDENT_BACK_NAV_BUTTON =
  'student-back-nav back-button inline-flex h-9 max-w-full items-center gap-2 rounded-full border px-2 py-1.5 text-sm font-medium sm:h-10 sm:gap-2.5 sm:px-2.5';

/** Panneau principal — liste complète des offres. */
export const INTERNSHIP_OFFERS_ALL_MAIN_SECTION =
  'student-all-offers-main-panel admin-module-panel flex w-full min-w-0 max-w-full flex-col gap-3 overflow-x-clip p-4 max-[429px]:gap-2.5 max-[429px]:p-3.5 sm:gap-4 sm:p-5';
