/** Layout — Encadrant chat fills the shell main when `contentFlush` is set. */

export const ENCADRANT_CHAT_FLUSH_ROOT =
  'font-inter flex h-full min-h-0 w-full min-w-0 max-w-none flex-1 flex-col overflow-hidden';
