/**
 * @deprecated Import from `shared/chat-design-system` instead.
 * Re-exports preserved for backward compatibility with internship module imports.
 */
export {
  formatChatSystemMessage as formatInternshipSystemMessage,
  parseSmartActionCode,
  shouldHideSmartActionForInbox,
  STUDENT_HIDDEN_SMART_ACTIONS,
} from '../../../../shared/chat-design-system/utils/chatSystemMessageUtils';
