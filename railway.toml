# Use when Railway Root Directory is empty (repo root pfe-talent-center).
[deploy]
preDeployCommand = []
startCommand = "cd backend && python manage.py migrate --noinput && python -m gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
